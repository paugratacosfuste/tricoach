# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files — never tracked. Only .env.example is allowed.
.env
.env.*
!.env.example

# Internal planning docs (not for public repo)
LAUNCH_PLAN.md

# Claude Code per-developer config (machine-local permissions, not project source)
.claude/

# Internal working agreement for Claude Code (not for public repo)
CLAUDE.md

# Project-specific skills used by Claude Code (internal patterns, gitignored)
skills/
